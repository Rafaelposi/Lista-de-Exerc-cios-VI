import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Definindo o número de alunos
        int numeroAlunos = 10;

        // Criando um vetor para armazenar as médias dos alunos
        double[] medias = new double[numeroAlunos];

        // Criando um scanner para ler a entrada do usuário
        Scanner scanner = new Scanner(System.in);

        // Pedindo as notas dos alunos e calculando as médias
        for (int i = 0; i < numeroAlunos; i++) {
            System.out.println("Digite as quatro notas do aluno " + (i + 1) + ":");
            double soma = 0;
            for (int j = 0; j < 4; j++) {
                System.out.print("Nota " + 
